<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/account/<%= employee._id %>?_method=put" method="POST">
        <label for="email">Enter new email</label>
        <input type="email" id="email" name="email" value="<%= employee.email %>" required>
        <label for="firstName">Enter new first name</label>
        <input type="text" id="firstName" name="firstName" value="<%= employee.firstName %>" required>
        <label for="secondName">Enter new second name</label>
        <input type="text" id="secondName" name="secondName" value="<%= employee.secondName %>" required>
        <label for="department">Choose new Department</label>
        <select name="department" id="department">
            <% for(let d of departments) {%>

            <option value="<%=d.name%>" <%= employee.department === d.name ? 'selected':'' %>><%=d.name%></option>
            <% } %> 
        </select>
        
        <button type="submit">update</button>
    </form>
    
</body>
</html>